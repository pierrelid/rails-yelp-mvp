<div class="container">
  <div class="row">
      <h2><%= @restaurant.name %></h2>
      <h3><%= @restaurant.phone %></h3>
      <h4><%= @restaurant.address %></h4>
      <%= link_to "ajouter review", new_restaurant_review_path(@restaurant)  %>
      <% @reviews.each do |review| %>
      <%= review.content %>
      <% end %>
    </div>
  </div>
</div>
